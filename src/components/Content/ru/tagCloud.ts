import { PageComponent } from '../getContent';

const tagCloud: PageComponent = {
  ps: 'Я предпочитаю WebStorm, а также иногда использую Vectornator, Figma, Pixelmator Pro и Terminal.',
};

export default tagCloud;
